{
  "name": "finalprojectnode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node app.js",
    "start:dev": "node app.js",
    "server": "nodemon server.js",
    "client": "PORT=8000 npm run start --prefix client",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "cross-env NODE_ENV=development concurrently \"npm run server\" \" PORT=8000 npm run client\"",
    "client:install": "npm install --prefix client",
    "client:build": "npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.3.4",
    "create-react-app": "^4.0.3",
    "cross-env": "^7.0.3",
    "express": "^4.17.1",
    "express-validator": "^6.10.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.11.18",
    "nodemon": "^2.0.7",
    "redux-thunk": "^2.3.0"
  },
  "devDependencies": {
    "concurrently": "^6.0.0",
    "node-sass": "^5.0.0",
    "react-redux": "^7.2.2",
    "sass-loader": "^11.0.1"
  }
}
